# ISSO Kiosk (iOS)

SwiftUI iPad app for in-person donations (Stripe Terminal).

**Open in Xcode:** `ISSOKiosk/ISSOKiosk.xcodeproj` — select an iPad simulator or device, then build (⌘R).

Project-wide setup, environment variables, and deployment notes are in the [repository root README](../README.md).
